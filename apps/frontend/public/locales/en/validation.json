{
  "required": "This field is required",
  "nameRequired": "Name is required",
  "minLength": "Must be at least {{min}} characters",
  "maxLength": "Must be at most {{max}} characters",
  "email": "Please enter a valid email address",
  "urlFormat": "Please enter a valid URL",
  "invalidFormat": "Invalid format",
  "serverName": {
    "required": "Server name is required",
    "invalidCharacters": "Server name must only contain letters, numbers, underscores, and hyphens",
    "consecutiveUnderscores": "Server name cannot contain consecutive underscores",
    "alreadyExists": "Server name already exists. Server names must be unique within your scope."
  },
  "command": {
    "required": "Command is required for stdio servers",
    "invalidCommand": "Please enter a valid command"
  },
  "url": {
    "required": "URL is required for SSE and Streamable HTTP types",
    "invalidUrl": "URL must be valid for SSE and Streamable HTTP types"
  },
  "namespaceName": {
    "required": "Namespace name is required",
    "invalidCharacters": "Namespace name must only contain letters, numbers, underscores, and hyphens"
  },
  "endpointName": {
    "required": "Endpoint name is required",
    "invalidCharacters": "Endpoint name must be URL compatible",
    "urlCompatible": "Name must be URL compatible"
  },
  "apiKeyName": {
    "required": "API key name is required"
  },
  "apiKeyScope": {
    "required": "Select the endpoint this API key is scoped to, or choose all endpoints",
    "exclusive": "Choose either a single endpoint or all endpoints, not both"
  },
  "apiKeyActsAs": {
    "requiresEndpoint": "An identity-bound key must be scoped to a single endpoint — pick a specific endpoint or remove the acts-as user",
    "ownerMismatch": "An identity-bound key must be owned by the user it acts as — a public or foreign-owned key cannot carry an identity binding"
  },
  "description": {
    "tooLong": "Description is too long"
  },
  "password": {
    "tooShort": "Password must be at least 8 characters long",
    "mismatch": "Passwords do not match"
  },
  "generic": {
    "invalid": "Invalid value",
    "required": "This field is required",
    "tooShort": "Too short",
    "tooLong": "Too long"
  },
  "maxRateSeconds": {
    "required": "Max rate seconds is required",
    "tooSmall": "Max rate must be greater than 0",
    "tooLarge": "Max rate must be less than 1000000"
  },
  "maxRate": {
    "required": "Max rate is required",
    "tooSmall": "Max rate must be greater than 0",
    "tooLarge": "Max rate must be less than 1000000"
  },
  "clientMaxRateSeconds": {
    "required": "Max rate seconds is required",
    "tooSmall": "Max rate must be greater than 0",
    "tooLarge": "Max rate must be less than 1000000"
  },
  "clientMaxRate": {
    "required": "Max rate is required",
    "tooSmall": "Max rate must be greater than 0",
    "tooLarge": "Max rate must be less than 1000000"
  }
}
